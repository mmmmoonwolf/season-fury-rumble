/**
 * ค่าของท่า "เรียกยมฑูต" (stand) — จูนได้ที่นี่ที่เดียว ไม่ต้องแตะ logic
 *
 * แนวคิดของท่านี้: ร่างที่เรียกออกมาต้องอ่านว่าเป็น "พลัง" ไม่ใช่ตัวละครอีกตัวที่มาร่วมฉาก
 * สิ่งที่ทำให้มันดูเป็นตัวละครจริงคือ (1) อยู่นาน (2) ทึบ (3) เท้าแตะพื้น
 * ค่าด้านล่างจึงตั้งมาสวนทางทั้งสามข้อ: อยู่สั้นมาก โปร่ง และลอยเหนือพื้นเสมอ
 */
export const STAND = {
  /** คูลดาวน์หลังใช้ (ms) — ยังไม่ผูกกับมิเตอร์อะไร ใช้เวลาล้วนไปก่อน */
  cooldown: 5000,

  // ---------- ไทม์ไลน์ (ms) ----------
  /** ช่วงจางเข้า — ยังไม่มี hitbox ผู้เล่นฝ่ายตรงข้ามมีเวลาเห็นแล้วหนี */
  appear: 170,
  /** เงื้อเคียวก่อนเหวี่ยง */
  startup: 90,
  /** ช่วงที่ hitbox เปิด = ช่วงที่คมเคียวกวาดผ่าน */
  active: 110,
  /** จางหายขึ้นข้างบน */
  dissolve: 220,
  /** เจ้าของท่าขยับไม่ได้กี่ ms (= ราคาของท่านี้ ถ้าพลาดจะโดนสวน) */
  ownerLock: 480,

  // ---------- ตำแหน่ง/ขนาด ----------
  /** จุดกึ่งกลางภาพร่าง วางห่างจากกลางลำตัวเจ้าของกี่ px (offsetX คูณทิศที่หันอยู่) */
  offsetX: 58,
  /**
   * ยกสูงขึ้นจากเดิม (-4 → -52) ให้รู้สึกว่า "ผุดออกมาจากไหล่" ไม่ใช่ยืนอยู่ข้าง ๆ เจ้าของท่า
   * ค่าติดลบ = สูงขึ้น (อ้างจากกลางลำตัวเจ้าของ)
   */
  offsetY: -52,
  /** ความสูงของ "ผืนภาพ" ร่างในโลกเกม (ตัวจริงในภาพสูงราว 88% ของผืน) */
  worldHeight: 240,
  /** ความสูงผืนภาพต้นฉบับใน atlas — ต้องตรงกับ CANVAS ใน make_reaper.py */
  sourceCanvasHeight: 480,
  /** ใช้ตอนยังไม่มี atlas จริง: สูงกว่าเจ้าของกี่เท่า (ยืมสไปรท์เจ้าของมาแสดงแทน) */
  placeholderHeightScale: 1.3,

  // ---------- ภาพ ----------
  visual: {
    /**
     * โปร่งแค่ไหน — ความเป็นวิญญาณส่วนใหญ่ถูกอบไว้ในภาพแล้วตอนแปลงด้วย make_reaper.py
     * (ย้อมฟ้าเทา + ไล่ความทึบตามความเข้มของเส้น + เรืองแสงรอบตัว) ตรงนี้จึงเหลือแค่จูนละเอียด
     */
    alpha: 0.92,
    /** ไม่ย้อมซ้ำ ภาพย้อมมาแล้ว — ตั้งเป็นสีอื่นได้ถ้าอยากเปลี่ยนโทนโดยไม่ต้อง gen ใหม่ */
    tint: 0xffffff,
    /** ADD ทำให้ภาพที่ย้อมมาแล้วสว่างจนหลุด — ปิดไว้ เปิดเฉพาะตอนใช้สไปรท์ placeholder */
    additive: false,
    /** ลอยขึ้นลงระหว่างอยู่ (px) */
    bob: 10,
    /** ร่างซ้อนจาง ๆ ข้างหลัง ให้ขอบสั่นไม่นิ่งเหมือน sprite ธรรมดา */
    afterimages: 1,
    afterimageOffset: 18,
    afterimageAlphaRatio: 0.22,
    /** เส้นโค้งของคมเคียว — ส่วนเดียวที่คมชัด เพราะเป็นสิ่งที่ต้องอ่านออก (= hitbox) */
    /** ตอนมีใบเคียวจริงในภาพแล้ว เส้นนี้เหลือหน้าที่เป็น "รอยที่คมเคียวลากผ่าน" เลยบางลง */
    arcEnabled: true,
    arcColor: 0xeaf7ff,
    arcWidth: 3,
    arcRadius: 118,
    /** องศาที่คมเคียวกวาด (จากบนลงล่างในทิศที่หันอยู่) */
    arcFromDeg: -105,
    arcToDeg: 35,
  },

  /** ค่าความแรง — โครงเดียวกับ BASIC_COMBO ใน combat.config เพื่อให้ CombatSystem อ่านได้ตรง ๆ */
  spec: {
    reach: 150,
    hitboxHeight: 170,
    hitboxYOffset: 30, // ใบเคียวกวาดต่ำกว่ากลางภาพ
    damage: 22,
    knockbackX: 340,
    knockbackY: -260,
    hitstun: 320,
  },
};

/**
 * ท่าตบพื้นฐานของ Bomb — ไม่ได้ต่อยเอง แต่ให้ร่างยมฑูตวาบออกมาฟันแทน
 *
 * ต่างจาก STAND (ท่าเรียกร่างเต็ม) 3 อย่าง:
 *  1. สั้นกว่ามาก — รวมไม่ถึง 0.3 วิ แค่ "วาบมาแล้วหาย" ไม่ใช่โผล่มายืน
 *  2. ใกล้ตัวกว่า — เป็น close combat ทับกับระยะหมัดปกติ
 *  3. ไม่มี hitbox ของตัวเอง — ดาเมจยังใช้ของ BASIC_COMBO เหมือนเดิม
 *     (แปลว่าบาลานซ์ที่จูนไว้ไม่เปลี่ยน ร่างเป็นภาพล้วน ๆ)
 */
export const STAND_SLASH = {
  /** ความสูงผืนภาพในโลกเกม (ผืนของท่าฟันคือ 420x340 เล็กกว่าท่าเรียกร่างเต็ม) */
  worldHeight: 135,
  sourceCanvasHeight: 340,
  /** จางเข้า / ค้าง / จางออก (ms) — รวมต้องไม่ยาวกว่า active+recovery ของหมัดนั้น */
  appear: 40,
  hold: 70,
  fade: 110,
  alpha: 0.85,
  /** ไม่ต้องเอียงแล้ว — เฟรมทั้งสามวาดมาคนละทิศอยู่แล้ว (ฟันลง / ฟันขึ้น / ฟันขวาง) */
  angles: [0, 0, 0],
  /** ระยะห่างจากกลางลำตัวเจ้าของ ต่อจังหวะที่ 1/2/3 */
  // ยกขึ้นทั้งชุดจากเดิมราว 25px — ฟันจากระดับไหล่ลงมา ไม่ใช่ฟันจากระดับเอว
  offsets: [
    { x: 58, y: -54 },
    { x: 64, y: -32 },
    { x: 70, y: -46 },
  ],
  /**
   * เฟรมต่อจังหวะ — ต่างกันที่ "ทิศทางรอยฟัน" (ลง → ขึ้น → ขวาง)
   * มี slash_thrust อีกเฟรมใน atlas ยังไม่ได้ใช้ เก็บไว้เผื่อท่าพุ่ง/ท่าอากาศทีหลัง
   */
  frames: ["slash_high", "slash_low", "slash_cross"],
};
